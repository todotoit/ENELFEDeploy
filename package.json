{
  "name": "fedeploy",
  "version": "1.0.28",
  "main": "index.js",
  "scripts": {
    "deploy_common": "git add -A && git commit -m \"deploy\" && npm version patch && git push",
    "deploy_fillergame": "node deploy.js fillergame && npm run deploy_common",
    "deploy_website": "node deploy.js website && npm run deploy_common",
    "deploy_tablet": "node deploy.js tablet && npm run deploy_common"
  },
  "dependencies": {
    "copy-dir": "^0.3.0",
    "replace": "^0.3.0"
  }
}
